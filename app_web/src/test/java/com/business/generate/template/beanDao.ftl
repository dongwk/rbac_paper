package ${(bean.package)!};

/**
 * @author ${(author.name)!}
 * @date ${(author.date)!}
 * @version ${(author.version)!}
 */
public interface ${(bean.name)!}Dao extends BaseMapper<${(bean.name)!}> {

}